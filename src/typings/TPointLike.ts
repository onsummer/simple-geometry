/**
 * @description 二维点类型，2 个数字构成的数组（元组）
 * @example
 * const point2d = [1.4, 0.8]
 */
type Point2DLike = [number, number]

/**
 * @description 三维点类型，3 个数字构成的数组（元组）
 * @example
 * const point3d = [1.4, 0.8, -0.4]
 */
type Point3DLike = [number, number, number]

export { Point2DLike, Point3DLike }
